# ---------------------------------------------------------------------------
# Secrets. Never commit these.
# ---------------------------------------------------------------------------
.env
.env.local
*.token
*.key
!.env.example

# ---------------------------------------------------------------------------
# Live vault data. This is the actual password database - it must never reach
# a git remote, even a private one.
# ---------------------------------------------------------------------------
vw-data/
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal
attachments/
sends/
rsa_key*
config.json

# ---------------------------------------------------------------------------
# Backups (they contain the vault, encrypted or not)
# ---------------------------------------------------------------------------
backups/
*.tar.gz
*.tar.gz.age
*.tar.gz.gpg

# ---------------------------------------------------------------------------
# Local noise
# ---------------------------------------------------------------------------
*.log
.DS_Store
Thumbs.db
